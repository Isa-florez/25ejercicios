def promedio_notas()
    while estudiante:
        nnom_estu = input("ingrese su nombre (para salir ingrese 1)")
        if nnom_estu = 1
            break
        elif
            nota1 = float(input("ingrese la primera nota: "))
            nota2 = float(input("ingrese la segunda nota:"))
            nota3 = float(input("ingrese la tercera nota:"))
            promedio = (nota1 + nota2 + nota3) / 3
            if promedio >= 3:
            print("aprobado")
            else:
            print("reprobado")
